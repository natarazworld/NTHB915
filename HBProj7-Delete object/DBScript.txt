CREATE TABLE "SYSTEM"."ACTOR" 
   (	"ACTORID" NUMBER(10,0) NOT NULL ENABLE, 
	"ACTORNAME" VARCHAR2(20 BYTE), 
	"ACTORADDRS" VARCHAR2(20 BYTE), 
	"PHONE" NUMBER, 
	"REMUNERATION" FLOAT(126), 
	 CONSTRAINT "ACTOR_PK" PRIMARY KEY ("ACTORID"))